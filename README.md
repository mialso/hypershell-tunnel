### pipe experiments

#### setup
```shell
npm run init:keyfile
```

#### server
```shell
# obtain public key for the client
cat ./keyfile
# start listen
npm run start:server
```

#### client
```shell
npm run 
```
